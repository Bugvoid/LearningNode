/*var exec = require('child_process').exec;

exec("pwd", function (err, stdout, stderr) {
    console.log(stdout)
    }
)*/
/*NAO FUNCIONA NO LINUX
var spaw = require('child_process').spawn
, pwd = spaw('pwd')

pwd.stdout.setEndcoding('ut8') // retornar uma String, senão será retornado um objeto Buffer
pwd.stdout.on('data', function(data){
    console.log(data)
});
*/





